# ─── MoltyDEX Frontend Configuration ──────────────────────────────────
# Copy this file to .env.local and fill in your values.

# Solana RPC — server-side only (used by /api/rpc proxy, never exposed to browser)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# MoltyDEX API URL (public — included in client bundle)
NEXT_PUBLIC_API_URL=https://api.moltydex.com

# Google Analytics (optional)
NEXT_PUBLIC_GA_ID=

# Solana network (mainnet-beta | devnet | testnet)
NEXT_PUBLIC_SOLANA_NETWORK=mainnet-beta
